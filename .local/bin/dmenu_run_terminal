#!/bin/sh

# REQUIRES:
#	sh
#	coreutils
#	dmenu

# Make sure to set the TERMINAL environment variable

choice="$(compgen -c | dmenu "$@")"
binary="$(awk '{print $1}' <<< "$choice")"
fullpath="$(command -v "$binary")"

$TERMINAL "$SHELL" -c "$choice; sleep forever"
